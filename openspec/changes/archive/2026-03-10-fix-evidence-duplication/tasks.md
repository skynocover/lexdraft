## 1. 共用常數抽取

- [x] 1.1 在 `strategyConstants.ts` 新增 `AUTO_HEADINGS` 常數和 `isAutoGenerated()` 函式
- [x] 1.2 修改 `templateRenderer.ts`，移除本地 `AUTO_HEADINGS` 定義，改為從 `strategyConstants.ts` import

## 2. Writer loop 過濾

- [x] 2.1 在 `briefPipeline.ts` 的 writer loop（L358）之前，過濾 `store.sections` 中 section 名稱命中 `AUTO_HEADINGS` 的項目

## 3. Prompt 層輔助

- [x] 3.1 在 `strategyConstants.ts` 的 `SECTION_RULES` 新增一條規則：禁止規劃證據方法段落

## 4. 驗證

- [x] 4.1 TypeScript 型別檢查通過（`npx tsc --noEmit`）
- [x] 4.2 Prettier 格式化（`npx prettier --write` 修改的檔案）
