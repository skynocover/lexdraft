# Cloudflare Workers 本地開發環境變數
# 複製此檔為 .dev.vars 並填入實際值
# wrangler dev 會自動讀取 .dev.vars

# 認證 Token（自訂，前端登入用）
AUTH_TOKEN=dev-token-change-me

# Cloudflare AI Gateway（Unified Billing）
# Account ID: Cloudflare Dashboard 右側欄可找到
CF_ACCOUNT_ID=your-cloudflare-account-id
# Gateway ID: AI Gateway 頁面建立後的名稱
CF_GATEWAY_ID=your-gateway-name
# API Token: dash.cloudflare.com → My Profile → API Tokens → 建立 Token（需有 AI Gateway 權限）
CF_AIG_TOKEN=your-cloudflare-api-token
